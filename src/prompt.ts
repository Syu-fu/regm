import { colors } from "cliffy/ansi/colors.ts";

export const errorColor = colors.bold.red;
export const matchColor = colors.bold.green;
export const promptColor = colors.bold.blue;

export const promptString = "> ";
